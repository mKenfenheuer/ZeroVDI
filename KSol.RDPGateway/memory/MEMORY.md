- [gfx-stall-mitm-findings](gfx-stall-mitm-findings.md) — GFX stall ROOT CAUSE (v6, Connect-Initial byte-diff vs mstsc+spec) = we advertise CS_MCS_MSGCHANNEL so host grants MCS channel 1007, but our client SKIPPED SC_MSGCHANNEL and never JOINED 1007 → host can't run network-autodetect → throttles GFX after ~3 frames. Fix = parse SC_MSGCHANNEL + join 1007. (v5 FRAME_ACK queueDepth=0-every-frame kept; SUSPEND/transport/MONITOR_LAYOUT roots were wrong.) Also rdpmitm decoder + --replay + gateway recorder.
- [gfx-dvc-v3-required](gfx-dvc-v3-required.md) — this host requires DVC v3 (nested ZGFX); answering v1 makes the Graphics channel go silent.
