@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Forgot your password?";
}
<h2 class="text-xl font-semibold text-center mb-1">@ViewData["Title"]</h2>
<p class="text-sm text-muted-foreground text-center mb-6">Enter your email address to reset your password.</p>
<form method="post" class="space-y-4">
    <div asp-validation-summary="ModelOnly" role="alert"></div>
    <div>
        <label asp-for="Input.Email" class="form-label">Email</label>
        <input asp-for="Input.Email" class="form-input" autocomplete="username" aria-required="true" placeholder="name@example.com" />
        <span asp-validation-for="Input.Email" class="form-error"></span>
    </div>
    <button type="submit" class="btn-primary w-full">Reset password</button>
    <a asp-page="./Login" class="block text-center text-sm text-primary hover:underline">Back to sign in</a>
</form>

@section Scripts { <partial name="_ValidationScripts" /> }
