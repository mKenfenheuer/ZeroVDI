@*
    Status/error messages from TempData["Status"] / TempData["Error"] are now surfaced as toast
    notifications by the layout's _Toasts partial (peeking TempData so this partial can stay a no-op).
    Kept as an empty partial so existing <partial name="_StatusMessage" /> references remain valid.
*@
