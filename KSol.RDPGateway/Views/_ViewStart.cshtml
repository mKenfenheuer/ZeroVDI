@{
    // Default layout for MVC views. Home (dashboard/privacy) opts into _UserLayout explicitly;
    // everything else here is admin/recordings management and uses the admin shell.
    Layout = "_AdminLayout";
}
