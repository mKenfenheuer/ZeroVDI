{
  "ConnectionStrings": {
    "DefaultConnection": "DataSource=Data/app_db.sqlite;Cache=Shared",
    "IdentityDataContextConnection": "Server=(localdb)\\mssqllocaldb;Database=KSol.ZeroVDI;Trusted_Connection=True;MultipleActiveResultSets=true"
  },
  "Recording": {
    "Directory": "Data/recordings",
    "DefaultEnabled": false,
    "_lifecycle_comment": "Lifecycle controls (roadmap #11). EncryptAtRest encrypts track files with AES-256-CTR keyed off DataProtection:MasterKeyPassphrase (seekable; losing the passphrase makes recordings unrecoverable). RetentionDays deletes recordings older than N days (0 = keep forever). MaxTotalGB caps total recordings size, purging oldest first (0 = no cap). RetentionSweepHours sets the purge cadence.",
    "EncryptAtRest": false,
    "RetentionDays": 0,
    "MaxTotalGB": 0,
    "RetentionSweepHours": 6
  },
  "Sessions": {
    "_comment": "MaxConcurrentPerUser caps simultaneous live console sessions per user (0 = unlimited). Redirect/handover continuation legs do not count against it.",
    "MaxConcurrentPerUser": 0
  },
  "Mfa": {
    "_comment": "Multi-factor enforcement. Identity always challenges users who have an authenticator enrolled; these settings decide who MUST enroll. RequireForAll forces every user; RequiredRoles forces members of the listed roles. If neither is set, MFA is required for Admins by default.",
    "RequireForAll": false,
    "RequiredRoles": [ "Admin" ]
  },
  "RateLimiting": {
    "_comment": "IP-based fixed-window throttles. 'Auth' covers the Identity login/register/password pages; 'Ws' covers the RDP WebSocket relay handshake. Rejected requests get HTTP 429.",
    "AuthPermitLimit": 30,
    "AuthWindowSeconds": 60,
    "WsPermitLimit": 45,
    "WsWindowSeconds": 60
  },
  "DataProtection": {
    "KeysDir": "Data/dp-keys",
    "_comment": "MasterKeyPassphrase encrypts the credential keyring at rest and is REQUIRED in Production. Do NOT set it here — supply it out-of-band via the DataProtection__MasterKeyPassphrase environment variable or a secret store.",
    "MasterKeyPassphrase": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning",
      "KSol.ZeroVDI": "Information",
      "Microsoft.AspNetCore.Authentication": "Information"
    }
  },
  "Smtp": {
    "_comment": "Outbound e-mail: password resets, e-mail-change confirmations and e-mail MFA codes. Leave Username/Password empty for an unauthenticated relay. Send a test message from Admin -> Operations once configured.",
    "Host": "smtp.example.com",
    "Port": 587,
    "Username": "",
    "Password": "",
    "FromAddress": "zerovdi@example.com",
    "FromName": "ZeroVDI",
    "UseSsl": true
  },
  "App": {
    "_comment": "The address users reach ZeroVDI on. Password-reset / e-mail-change links and the OpenID Connect redirect URI are built from it instead of the request's Host header, which a forged request could otherwise point elsewhere. REQUIRED in production.",
    "PublicBaseUrl": ""
  },
  "_AllowedHosts_comment": "Set this to the host name(s) this deployment answers on, e.g. 'vdi.example.com'. '*' accepts any Host header.",
  "AllowedHosts": "*"
}