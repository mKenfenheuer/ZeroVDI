@model KSol.ZeroVDI.ViewComponents.DocsNavModel
@if (Model.Nodes.Any())
{
    <nav class="doc-nav px-1">
        <partial name="_DocNav" model="@((Model.Nodes, Model.CurrentUrl, Model.UrlBase))" />
    </nav>
}
