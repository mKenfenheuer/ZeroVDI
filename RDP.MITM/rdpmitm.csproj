<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Link the gateway's RDP auth classes directly instead of project-referencing the Web SDK app. -->
    <RootNamespace>KSol.RDPGateway.RDP</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="../KSol.RDPGateway/RDP/CredSspClient.cs" />
    <Compile Include="../KSol.RDPGateway/RDP/NtlmClient.cs" />
    <Compile Include="../KSol.RDPGateway/RDP/Ntlm.cs" />
    <Compile Include="../KSol.RDPGateway/RDP/AuthCrypto.cs" />
    <Compile Include="../KSol.RDPGateway/RDP/KerberosAuth.cs" />
  </ItemGroup>

  <!-- Shared RDP wire decode/encode/dump engine (also used by the gateway). -->
  <ItemGroup>
    <ProjectReference Include="../RdpWire/KSol.RDP.Wire.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Microsoft.Extensions.Logging" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Kerberos.NET" Version="4.6.146" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="9.0.0" />
  </ItemGroup>

</Project>
