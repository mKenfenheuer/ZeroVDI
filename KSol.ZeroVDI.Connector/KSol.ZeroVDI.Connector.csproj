<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Shared with the gateway so both probe RDP hosts the same, GNOME-Remote-Desktop-safe way. -->
    <Compile Include="..\KSol.ZeroVDI\RDP\RdpPortProbe.cs" Link="RdpPortProbe.cs" />
  </ItemGroup>

</Project>
